diff --git a/node_modules/@expo/metro-config/build/serializer/serializeChunks.js b/node_modules/@expo/metro-config/build/serializer/serializeChunks.js
index 10c8ce3..1b230a3 100644
--- a/node_modules/@expo/metro-config/build/serializer/serializeChunks.js
+++ b/node_modules/@expo/metro-config/build/serializer/serializeChunks.js
@@ -376,6 +376,7 @@ class Chunk {
             });
         }
         if (this.boolishTransformOption('bytecode') && this.isHermesEnabled()) {
+            if (jsAsset.metadata.expoDomComponentReferences?.length) { jsAsset.metadata.deferredHermesBytecode = true; } else {
             const adjustedSource = jsAsset.source.replace(/^\/\/# (sourceMappingURL)=(.*)$/gm, (...props) => {
                 if (props[1] === 'sourceMappingURL') {
                     const mapName = props[2].replace(/\.js\.map$/, '.hbc.map');
@@ -415,7 +416,7 @@ class Chunk {
                     : hermesBundleOutput.sourcemap;
                 assets[1].filename = assets[1].filename.replace(/\.js\.map$/, '.hbc.map');
             }
-        }
+        } }
         return assets;
     }
     supportsBytecode() {
diff --git a/node_modules/@expo/cli/build/src/export/exportApp.js b/node_modules/@expo/cli/build/src/export/exportApp.js
index 01163de..c727846 100644
--- a/node_modules/@expo/cli/build/src/export/exportApp.js
+++ b/node_modules/@expo/cli/build/src/export/exportApp.js
@@ -273,6 +273,22 @@ async function exportAppAsync(projectRoot, { platforms, outputDir, clear, dev, d
                         })
                     ];
                 }));
+                for (const artifact of bundle.artifacts){
+                    if (artifact.type !== 'js' || !artifact.metadata.deferredHermesBytecode) continue;
+                    delete artifact.metadata.deferredHermesBytecode;
+                    const { buildHermesBundleAsync } = require('@expo/metro-config/build/serializer/exportHermes');
+                    const prevName = artifact.filename;
+                    const entry = files.get(prevName);
+                    const code = (entry ? entry.contents : artifact.source).toString();
+                    const adjusted = code.replace(/^\/\/# (sourceMappingURL)=(.*)$/gm, (...props)=>{
+                        if (props[1] === 'sourceMappingURL') return `//# ${props[1]}=` + props[2].replace(/\.js\.map$/, '.hbc.map');
+                        return '';
+                    });
+                    const out = await buildHermesBundleAsync({ projectRoot, code: adjusted, map: null, minify: true, filename: artifact.originFilename });
+                    artifact.source = out.hbc;
+                    artifact.filename = prevName.replace(/\.js$/, '.hbc');
+                    if (entry) { files.delete(prevName); files.set(artifact.filename, { ...entry, contents: out.hbc }); }
+                }
                 if (platform === 'web') {
                     const faviconAsset = await (0, _favicon.generateFaviconAssetAsync)(projectRoot, {
                         outputDir,
